/*switch (global.current_act)
{
    case 0:
        if (act_score < 10000)
        {
            act_rank = "F";
        }
        else if (act_score >= 10000 && act_score < 20000)
        {
            act_rank = "D";
        } 
        else if (act_score >= 20000 && act_score < 30000)
        {
            act_rank = "C";
        }
        else if (act_score >= 30000 && act_score < 40000)
        {
            act_rank = "B";
        }
        else if (act_score >= 40000 && act_score < 45000)
        {
            act_rank = "A";
        }
        else if (act_score >= 45000 && act_score < 60000)
        {
            act_rank = "S";
        }
        break;
}
*/
